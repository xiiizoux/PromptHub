# Run lint-staged for file-level checks
npx lint-staged

# MCP Server checks
echo "🔍 检查 MCP 服务器..."
cd mcp
npm run lint
npm run typecheck
cd ..

# Web Frontend checks  
echo "🔍 检查 Web 前端..."
cd web
npm run lint:strict
npm run typecheck
cd ..

echo "✅ 所有检查通过！"
